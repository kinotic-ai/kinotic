{
    "name": "@kinotic-ai/spawn-renderer",
    "version": "0.0.0",
    "private": true,
    "description": "Bundles the @kinotic-ai/spawn engine into a self-contained GraalJS script embedded in kinotic-github.",
    "license": "Elastic License 2.0",
    "type": "module",
    "scripts": {
        "build": "NODE_PATH=node_modules esbuild src/entry.ts --bundle --platform=browser --format=iife --global-name=KinoticSpawn --outfile=dist/spawn-renderer.js --log-level=warning"
    },
    "dependencies": {
        "liquidjs": "10.25.7",
        "zod": "^4.3.6"
    },
    "devDependencies": {
        "esbuild": "^0.27.4"
    }
}
