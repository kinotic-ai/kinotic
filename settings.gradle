pluginManagement {
    repositories {
        gradlePluginPortal()
        mavenCentral()
    }
}

dependencyResolutionManagement {
    repositories {
        mavenCentral()
        maven {
            url = uri('https://s01.oss.sonatype.org/content/repositories/snapshots/')
        }

    }
}

rootProject.name = 'structures'


rootDir.listFiles().each {
    if (it.directory && new File(it, 'settings.gradle').exists()) {
        include ":${it.name}"
    }
}

// Only include for local development
// if (file('../continuum-framework').exists()) {
//     includeBuild('../continuum-framework') {
//         dependencySubstitution {
//             substitute module('org.kinotic:continuum-core') using project(':continuum-core')
//             substitute module('org.kinotic:continuum-core-vertx') using project(':continuum-core-vertx')
//             substitute module('org.kinotic:continuum-gateway') using project(':continuum-gateway')
//             substitute module('org.kinotic:continuum-idl') using project(':continuum-idl')
//             substitute module('org.kinotic:continuum-util') using project(':continuum-util')
//         }
//     }
// }