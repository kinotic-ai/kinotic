import { EntityRepository, type IEntitiesRepository } from '@kinotic-ai/persistence'
import { Alert } from '../../domain/Alert.js'


/**
 * Base Repository for interacting with Alert entities
 * This class was generated by the Kinotic CLI. And will be overwritten if the CLI is run again.
 */
export class BaseAlertRepository extends EntityRepository<Alert> {

  private readonly shouldValidate: boolean

  constructor(shouldValidate: boolean = true, entitiesRepository?: IEntitiesRepository) {
    super('kinotic-test', 'vBTFHVuFwd', 'Alert', entitiesRepository)
    this.shouldValidate = shouldValidate
  }

  protected async beforeSaveOrUpdate(entity: Alert): Promise<Alert> {
    if (this.shouldValidate) {
      return this.validate(entity)
    } else {
      return entity
    }
  }

  protected async beforeBulkSaveOrUpdate(entities: Alert[]): Promise<Alert[]> {
    if (this.shouldValidate) {
      const validatedEntities: Alert[] = []
      for (let entity of entities) {
        validatedEntities.push(this.validate(entity))
      }
      return validatedEntities
    } else {
      return entities
    }
  }

  validate(entity: Alert): Alert {
    let ret: any
    if (entity) {
      ret = (ret ? ret : {})
      ret.alertId = entity.alertId
      ret.message = entity.message
      ret.severity = entity.severity
      ret.timestamp = entity.timestamp
      ret.source = entity.source
      ret.active = entity.active
    }

    return ret
  }

}
