{
  "name": "{{projectName}}",
  "scripts": {
    "build": "bunup",
    "dev": "bunup --watch",
    "type-check": "bun run --filter '*' type-check"
  },
  "devDependencies": {
    "@types/bun": "^1.3.10",
    "bunup": "^0.16.31",
    "typescript": "^5.9.3"
  },
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "type": "module"
}
