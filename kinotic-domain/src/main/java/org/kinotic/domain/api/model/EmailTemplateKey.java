package org.kinotic.domain.api.model;

/**
 * The kinds of email an application can customize with an {@link EmailTemplate}. One
 * template may exist per application and key.
 */
public enum EmailTemplateKey {

    /** The invitation email sent when someone is invited into the application. */
    INVITATION
}
