---
name: entities-and-persistence
description: >
  How to define and persist Kinotic domain models: entity classes with @Entity,
  @AutoGeneratedId or @Id, and field decorators; generating typed repositories with
  bun run generate; repository CRUD and search operations; named queries with @Query;
  SQL migrations; multi-tenancy. Use when creating or changing entities, domain models,
  repositories, queries, schema, migrations, or stored data in a Kinotic project.
---

# Entities and Persistence

Kinotic persistence is code-first: entities are plain TypeScript classes with
decorators, and typed repository classes are generated from them. Exact decorator
signatures and the full repository API are in `references/decorators.md`. Deeper
documentation: <https://kinotic.ai/apps/persistence/overview>.

## Where things live

`.config/kinotic.config.ts` names the directories (`entitiesPaths[].path` for entity
classes, `repositoryPath` for generated repositories). In the standard scaffold that is
`packages/domain/model` and `packages/domain/repositories` — but always trust the
config file in the repository.

## Defining an entity

```typescript
import { Entity, AutoGeneratedId, NotNull, Text, Precision, PrecisionType, MultiTenancyType } from '@kinotic-ai/persistence'

@Entity(MultiTenancyType.NONE)
export class Person {
    @AutoGeneratedId
    id: string | null = null

    @NotNull
    firstName: string = ''

    @NotNull
    lastName: string = ''

    @Text
    bio: string = ''

    @Precision(PrecisionType.SHORT)
    age: number = 0
}
```

Rules:

- **Exactly one id field**: `@AutoGeneratedId` on `id: string | null = null` (server
  assigns on save) or `@Id` on a `string = ''` field you populate yourself.
- Give properties defaults so entities serialize round-trip cleanly; optional (`?`) and
  definitely-assigned (`!`) properties are also accepted.
- Strings are exact-match (keyword) indexed unless marked `@Text` (full-text). Numbers
  default to 32-bit INT unless `@Precision(...)` says otherwise.
- Decorators are compile-time markers read from the source — they have no runtime
  behavior, so entity classes stay plain data.
- For time-series data use `@Entity(MultiTenancyType.NONE, EntityType.STREAM)` with one
  `@TimeReference` timestamp field.

## The change workflow

1. Create or edit entity classes under the configured entities path.
2. Run `bun run generate` from the project root — regenerates
   `generated/Base<Entity>Repository` for every entity, creates `<Entity>Repository`
   (once) for new ones, and writes each entity's schema to
   `.config/c3/entities/<namespace>.<name>.json` (named queries to
   `.config/c3/queries/<RepositoryName>.json`). Local only; no server connection.
3. Commit and push everything generate produced — including `.config/c3` — and
   Kinotic OS synchronizes entity definitions from the connected GitHub repository.
   Never run `kinotic login` or `kinotic sync` yourself.

Only the `Base*` classes are regenerated; the `<Entity>Repository` subclass is created
once and never overwritten, so custom code (like named queries) belongs there.

## Using repositories

```typescript
import { Kinotic, Pageable } from '@kinotic-ai/core'
import { PersistencePlugin } from '@kinotic-ai/persistence'
import { PersonRepository } from '../repositories/PersonRepository.js'

Kinotic.use(PersistencePlugin)
await Kinotic.connect({ host: 'localhost', port: 58503 })

const people = new PersonRepository()

const saved = await people.save({ id: null, firstName: 'Jane', lastName: 'Doe', bio: '', age: 28 })
const page = await people.findAll(Pageable.create(0, 25))   // page.content, page.totalElements
const hits = await people.search('Jane', Pageable.create(0, 10))  // matches @Text fields
```

Full surface: `save`, `bulkSave`, `update` (partial — only fields present change),
`bulkUpdate`, `findById` (null when missing), `findByIds`, `findAll`, `search`,
`deleteById`, `deleteByQuery`, `count`, `countByQuery`, `syncIndex`. Signatures in
`references/decorators.md`.

Writes are not instantly searchable; call `await people.syncIndex()` in tests or
scripts that query right after writing.

## Named queries

Custom aggregate queries go on the `<Entity>Repository` subclass (never the generated
base) with `@Query` and an empty body — `bun run generate` fills the implementation:

```typescript
import { Query } from '@kinotic-ai/persistence'

export class OrderRepository extends BaseOrderRepository {

    @Query('SELECT COUNT(*) FROM Order WHERE status = :status')
    countByStatus(status: string): Promise<number> {
        // Implementation is generated by the CLI
    }
}
```

Only aggregates (COUNT, SUM, AVG, MIN, MAX) are supported today. Parameters bind by
`:name`. A `Pageable` parameter, when used, should be first or last. Queries must be
synchronized to the server (push) before they work at runtime.
Docs: <https://kinotic.ai/apps/persistence/named-queries>.

## Migrations

Schema changes beyond adding entities/fields are explicit SQL files in `./migrations`,
named `V<N>__<description>.sql` (e.g. `V2__add_status_index.sql`), executed in order
during server synchronization and recorded so they never re-run. The dialect covers
CREATE TABLE / DATA STREAM, COMPONENT and INDEX TEMPLATE, ALTER TABLE ADD COLUMN,
REINDEX, INSERT, UPDATE, DELETE.
Grammar: <https://kinotic.ai/apps/reference/migration-sql-grammar>.

## Multi-tenancy

`@Entity(MultiTenancyType.SHARED)` plus one `@TenantId` field makes the generated
repository transparently tenant-scoped — the tenant id is populated from the
authenticated user and every query is filtered to it. Generation also emits an
`<Entity>AdminRepository` whose methods take a leading tenant-selection argument
(`['*']` for all tenants, or a list of tenant ids) for cross-tenant administration.
Docs: <https://kinotic.ai/apps/persistence/multi-tenancy>.

## Further reading

- Entity definition guide: <https://kinotic.ai/apps/persistence/defining-entities>
- Field decorators: <https://kinotic.ai/apps/persistence/entity-decorators>
- CRUD operations: <https://kinotic.ai/apps/persistence/crud-operations>
- Full decorator reference: <https://kinotic.ai/apps/reference/decorators>
