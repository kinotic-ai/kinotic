# CI-specific test configuration
# Uses LOCAL discovery mode (single-node) which doesn't require specific IP addresses
# This overrides the cluster-specific settings in application-test.yml

continuum:
  cluster:
    discoveryType: LOCAL
    # Use defaults for ports - no need to specify localAddress in CI
    localAddress:
    localAddresses:
