apiVersion: batch/v1
kind: Job
metadata:
  name: {{ include "kinotic-server.fullname" . }}-migration-{{ .Release.Revision }}
  labels:
    app: {{ include "kinotic-server.name" . }}-migration
    release: {{ .Release.Name }}
  annotations:
    "helm.sh/hook": pre-install,pre-upgrade
    "helm.sh/hook-weight": "-5"
    "helm.sh/hook-delete-policy": before-hook-creation
spec:
  backoffLimit: {{ .Values.migration.backoffLimit | default 3 }}
  activeDeadlineSeconds: {{ .Values.migration.activeDeadlineSeconds | default 300 }}
  template:
    metadata:
      labels:
        app: {{ include "kinotic-server.name" . }}-migration
    spec:
      restartPolicy: Never
      containers:
        - name: kinotic-migration
          image: {{ if .Values.migration.image.sha }}"{{ .Values.migration.image.repository }}@{{ .Values.migration.image.sha }}"{{ else }}"{{ .Values.migration.image.repository }}:{{ .Values.migration.image.tag }}"{{ end }}
          imagePullPolicy: {{ .Values.migration.image.pullPolicy }}
          env:
            - name: KINOTIC_MIGRATION_ELASTIC_SCHEME
              value: "{{ (index .Values.kinotic.elastic.connections 0).scheme }}"
            - name: KINOTIC_MIGRATION_ELASTIC_HOST
              value: "{{ (index .Values.kinotic.elastic.connections 0).host }}"
            - name: KINOTIC_MIGRATION_ELASTIC_PORT
              value: "{{ (index .Values.kinotic.elastic.connections 0).port }}"
            - name: KINOTIC_MIGRATION_ELASTIC_USERNAME
              value: "{{ .Values.kinotic.elastic.username }}"
            - name: KINOTIC_MIGRATION_ELASTIC_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.kinotic.elastic.secretName }}
                  key: {{ .Values.kinotic.elastic.secretKey }}
