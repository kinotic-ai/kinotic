apiVersion: v1
kind: ServiceAccount
metadata:
  name: es-secret-sync
  namespace: {{ .Values.target.namespace }}
  annotations:
    "helm.sh/hook": pre-install,pre-upgrade
    "helm.sh/hook-weight": "-20"
