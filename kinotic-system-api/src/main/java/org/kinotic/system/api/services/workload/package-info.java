/**
 * Node and workload orchestration contracts, including the interfaces published for remote access and the proxy to a node's vm-manager.
 */
@Version("1.0.0")
@Zone(DomainUtil.SYSTEM_API_ZONE)
package org.kinotic.system.api.services.workload;

import org.kinotic.core.api.annotations.Version;
import org.kinotic.core.api.annotations.Zone;
import org.kinotic.domain.api.utils.DomainUtil;
