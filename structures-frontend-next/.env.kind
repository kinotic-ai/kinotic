# Kind cluster environment variables
# Use these when testing with kind: npm run dev -- --mode kind

# Continuum WebSocket connection settings for kind cluster
# Kind maps NodePort 30503 to host port 58503
# VITE_CONTINUUM_PORT=30503
VITE_CONTINUUM_PORT=80
